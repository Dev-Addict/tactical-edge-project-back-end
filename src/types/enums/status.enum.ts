/**
 * Enum for status
 * @readonly
 * @enum {string}
 * @property {string} FAIL - Fail status.
 * @property {string} ERROR - Error status.
 * @property {string} SUCCESS - Success status.
 */
export enum Status {
	FAIL = 'FAIL',
	ERROR = 'ERROR',
	SUCCESS = 'SUCCESS',
}
